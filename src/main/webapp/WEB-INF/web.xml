<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>AyeshaMart</display-name>

    <listener>
        <listener-class>com.ayeshamart.listener.DatabaseListener</listener-class>
    </listener>

    <!-- H2 web console: http://localhost:8080/ayeshamart/h2-console -->
    <servlet>
        <servlet-name>H2Console</servlet-name>
        <servlet-class>org.h2.server.web.WebServlet</servlet-class>
        <load-on-startup>2</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>H2Console</servlet-name>
        <url-pattern>/h2-console/*</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>H2Console</servlet-name>
        <url-pattern>/h2-console</url-pattern>
    </servlet-mapping>

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <name>JSESSIONID</name>
            <path>/</path>
            <http-only>true</http-only>
            <secure>false</secure>
        </cookie-config>
        <!--
          Tracking mode not restricted to COOKIE:
          the H2 web console threads its frame navigation with
          session ids in URLs (?jsessionid=...), so locking Tomcat
          to cookie-only tracking makes the console drop its login
          state and bounce back to the Connect page. Tomcat's default
          (sequential) keeps cookies primary while still honouring
          the console's URL session ids.
        -->
    </session-config>

</web-app>